server.port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/clarusway/petclinic-microservices.git
          default-label: main
---
spring:
  profiles: local
  cloud:
    config:
      server:
        git:
          uri: file:///${GIT_REPO}
